# ReminderGray

一款面向 Android 的本地四象限待办应用，根据重要性和紧急程度管理任务。当前实机验收版本为 `1.1.0`。

## 当前功能

- 四象限分别对应：立即做、安排做、委托做、尽量不做。
- 使用重要、紧急两个开关添加待办，也可以把输入中的草稿直接拖入任意象限。
- 支持可选日期与时间；不选择时不会保存或显示空时间。
- 主界面每个象限最多显示 4 条，点击右上角总数可查看全部待办。
- 详情页支持新增、编辑、完成、删除、调整时间和调整优先级。
- 已有待办可在主界面拖动到其他象限。
- 数据保存在本机，升级审核版时保留已有待办。
- 桌面组件可快速打开输入界面，并自动聚焦输入框。

## 第二阶段视觉

- 用户提供的图片分别用于应用图标和主界面背景。
- 四象限使用高透明度、偏灰色调的红、青、蓝、灰有色玻璃效果。
- 象限只保留待办、总数和背景行动提示，减少无关内容。
- 行动提示使用霞鹜文楷 Lite；字体及 OFL 许可随应用一起打包。
- 重要、紧急开关直接使用用户提供的参考图中开启和关闭两种按钮贴图，保留银色旋钮的金属光泽。
- 应用配色、交互与组件风格在后续版本中保持一致。

## 构建与运行

1. 使用 Android Studio 打开项目根目录。
2. 等待 Gradle 同步完成。
3. 选择 Android 8.0（API 26）或更高版本的设备。
4. 运行 `app` 配置。

调试 APK 位于 `app/build/outputs/apk/debug/app-debug.apk`。

## 桌面组件

安装并至少打开一次应用后，在桌面长按进入组件列表，找到 `ReminderGray`，添加“ReminderGray · 快速添加”组件。

## 实施阶段

- 第一阶段：基础 Android 功能，已完成并标记 `v1.0.0`。
- 第二阶段：视觉与交互美化，beta4 已完成并进入阶段提交。
- 第三阶段：电脑、平板、手机同步后端，按最新目标开始实施。
- 第四阶段：完整四象限桌面小部件，在第三阶段后实施。

原始网页原型保留在 `eisenhower-matrix.html`。
